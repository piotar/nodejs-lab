
klonowanie repo			                        	    git clone https://github.com/piotar/nodejs-lab.git
zmiana katalogu na lab09		                        cd .\nodejs-lab\
na masterze nie pracujemy! 
jedynie bierzemy z niego zmiany!
tworzymy branch					            	        git checkout -b daniel.iwaszko
pokazuje na jakim branchu jestesmy (lokalnie!)	        git branch
sprawdzamy status					                    git status
wysyłamy 1 zmiany z parametrami	na repo prowadzacego	git push --set-upstream origin daniel.iwaszko
nastepne pushowanie moze byc bez parametrow		        git push

dodajemy cały katalog do sledzenia (.)                  git add .
                                                        git status
zacomitowanie zmian                                     git commit -m 'testowy commit'
wrzucenie wszystkich commitow na branch zdalnie         git push (lub git push --set-upstream origin daniel.iwaszko)


??? 55:00                                                        git merge
przed mergem trzeba zacommitowac lub wrzucic do stash zmiany !   git commit -m 'cost tam'
pobiera do swojego repo (brancha) zmiany z innego brancha        git merge origin/hanna.cylwik
zazwyczaj przy git merge pojawiaja sie konflikty i trzeba je rozwiazac (wybrac wersje, ktora chcemy miec (wtyczka gitlens)
po rozwiazaniu konfliktow (wybraniu wlasciwej wersji) dodajemy   git add .
a nastepnie                                                      git commit -m 'tresc'
jezeli wyswietli sie vim to wyjsc mozna                          :q
pobieranie wszystkich zmian / branchy                            git fetch --all
przelaczenie sie na inne branche                                 g
zmieniamy katalog na konkretne laboratorium (do pracy)  cd lab09

instalacja:
    npm i
    npm i express
    npm i nodemon
    nodemon app.js

korzystamy z postman do sprawdzania nagłówków (headerów)
w headers możemy wysyłać dane   key = Authorization     value = alamakota
w przegladarce w console jest funkcja (fech ktora jest promisem) fetch("http://localhost:4000").then(res => console.log(res)) - zwroci nam statuscode nieautoryzowany
natomiast jak przeslemy w consoli przegldarki  fetch("http://localhost:4000", {headers: {authorization: "alamakota"}}).then(res => console.log(res)) - dostaniemy autoryzacje


funkcja do przekonwertowania base64 na stringa w przegladarce w konsoli w "Basic Auth"    atob('ciag znaków')
jest jeszcze Bearer Token - tez popularny

do zad 3 w przegladarce     fetch("http://localhost:4000", {headers: {authorization: "Daniel:mojehaslo"}}).then(res => console.log(res))

do zad 4 instalacja:
        npm i body-parser
        w postman wpisujac w body -> raw wpisujac "ala ma cukierki" wyrzuci zakazane slowo, a jak "wpiszemy ala ma cukierka" to wszystko bedzie ok